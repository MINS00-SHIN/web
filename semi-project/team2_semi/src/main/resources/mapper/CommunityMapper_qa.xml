<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team2.healthsns.mapper.CommunityMapper">

    <insert id="communityInsertQa">
        insert into board(title, content, writer, board_cat)
        values(#{title}, #{content}, #{writer}, 'qa');
    </insert>

    <select id="communityPageListQa" resultType="com.team2.healthsns.vo.CommunityVO">
        select post_id, title, writer, views, date_format(write_date,'%m-%d %h:%i') as writedate
        from board
        where board_cat = 'qa'
        <if test="searchWord != null and searchWord != ''">
            AND title like CONCAT('%', #{searchWord}, '%')
        </if>
        order by post_id desc
        limit #{onePageRecord}
        offset #{offsetPoint}
    </select>

    <select id="totalRecordQa">
        select count(post_id) from board
        where board_cat = 'qa'
        <if test="searchWord != null and searchWord != ''">
            AND title like CONCAT('%', #{searchWord}, '%')
        </if>
    </select>

    <select id="communitySelectQa" resultType="com.team2.healthsns.vo.CommunityVO">
        select post_id, title, writer, content, views, date_format(write_date,'%m-%d %h:%i') as writedate
        from board
        where post_id = #{param1}
    </select>

    <update id="hitCountQa">
        update board
        set views = views + 1
        where post_id = #{param1}
    </update>

    <update id="communityUpdateQa">
        update board
        set title = #{title}, content = #{content}
        where post_id = #{post_id}
    </update>

    <update id="communityDeleteQa">
        delete from board
        where post_id = #{param1}
    </update>

</mapper>
